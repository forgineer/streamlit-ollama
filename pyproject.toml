[project]
name = "streamlit-ollama"
version = "0.1.0"
description = "A simple Ollama chat app build with Streamlit."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ollama>=0.6.0",
    "streamlit>=1.50.0",
]
